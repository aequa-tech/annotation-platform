.list-group.mb-3
  .list-group-item
    = render Admin::FieldsetComponent.new(form, :fullname, :text, required: true)
  .list-group-item
    = render Admin::FieldsetComponent.new(form, :email, :text, required: true)
  .list-group-item
    = render Admin::FieldsetComponent.new(form, :editor_id, :association_search, required: true, association_name: :editor, search_path: admin_editors_path(name_field: :fullname), name_field: :editor_fullname)
  .list-group-item
    = render Admin::FieldsetComponent.new(form, :password, :password, required: true)
